{# templates/creneau/new.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Nouveau Créneau{% endblock %}

{% block body %}
    <h1>Créer un nouveau Créneau</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        
        <button type="submit" class="btn btn-primary">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('creneau_index') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}